# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart

# IDE
.idea/
.vscode/
*.iml

# Platform folders (android/ios는 AdMob 등 실제 설정을 담고 있어 추적함;
# 나머지는 `flutter create .`로 나중에 생성)
web/
windows/
linux/
macos/

# Misc
.DS_Store
*.log

# 아이콘/스플래시 만들 때 쓴 1회성 스크립트 및 중간 산출물 (assets/icon/의
# 결과물만 실제로 앱에서 씀, 개인 다운로드 경로를 하드코딩하고 있어 비공용)
tool/
